name: CI/CD Pipeline for MongoDB

on:
  push:
    branches:
      - main
      - add-tests
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 21

      - name: Install dependencies
        run: npm install

      - name: Run Server and Wait for it to start and start testing
        run: |
          npm run t
        env:
          MONGO_URI: mongodb+srv://m001-student:m001-student@main.0030e.mongodb.net/loginService
          CLIENT_EMAIL: ${{ secrets.CLIENT_EMAIL }}
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
          BASE_URL: http://localhost:5005
          IS_TEST: TRUE
          TEST_USER_NAME: adminUser
          TEST_USER_PWD: $2b$12$EfnHl3cYsaFgAQwFjv.Qee7vePCWWKloRoSRG3uiJOuEkkB0F7xBm
